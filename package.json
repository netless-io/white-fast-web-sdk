{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.16.5"
  },
  "dependencies": {
    "global": "^4.4.0"
  },
  "scripts": {
    "dev": "lerna run --parallel dev",
    "build:lib": "lerna run --parallel build:lib",
    "boot": "lerna bootstrap"
  },
  "workspaces": [
    "packages/*"
  ]
}
